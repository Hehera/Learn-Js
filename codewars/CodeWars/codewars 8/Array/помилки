  if (parts === "" || parts === "1" || parts === "1,2") {
    return "NULL";
  } /*неправильна умова бо я масив порівнюю з рядком тобто типи данних ,треба використовувати методи для перевірки елементів в масиві,
    для перевірки використовуємо indexOf,includes*/


    if (i === (goose - 1) % players.length) {}
    
    // % players.length - ми пишемо це коли у нас передаюча цифра більша за к-сть елементів в масивів не було underfined а просто виконався 2 обхід цикла
